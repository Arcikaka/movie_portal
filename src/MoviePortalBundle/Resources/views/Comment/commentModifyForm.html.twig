{% extends '@MoviePortal/base.html.twig' %}

{% block content %}
    <form method="post" action="{{ path('modify_comment_save', {'commentId' : comment.id}) }}">
        <input type="text" name="content" placeholder="{{ comment.content }}" width="500" height="200">
        <input type="submit" value="send">
    </form>
{% endblock %}