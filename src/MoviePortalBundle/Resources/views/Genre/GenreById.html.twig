{% extends '@MoviePortal/base.html.twig' %}
{% block content %}
<div>
    <h3> Genre {{ genre.name }}</h3>
</div>
<div>
    Movies:
    {% for movie in genre.movies %}
    <a href="{{ path('movie_by_id', {'id' : movie.id}) }}">{{ movie.title }}</a>
    {% endfor %}
</div>
{% endblock %}